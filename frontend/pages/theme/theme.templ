package theme

const (
    DefaultBGColor = "bg-white dark:bg-gray-900"
    DefaultTextColor = "text-gray-900 dark:text-gray-100"
)

type Theme struct {
	BGColor string
	TextColor string
}

templ A () {
}
